# Backend Agent

Scope:
- API routes, Prisma schema, scheduler, queue ops
- Server-side auth and access control

Constraints:
- Update tests when behavior changes
- No destructive data changes without [DESTRUCTIVE-OK]
