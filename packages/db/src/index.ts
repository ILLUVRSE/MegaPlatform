/**
 * Prisma client export for the monorepo.
 * Guard: server-side only. Do not import into client components.
 */
export { prisma } from "./prismaClient";
export { PrismaClient, Prisma } from "@prisma/client";
